<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>MiniSecBGP</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="A Lightweight and Distributed Testbed for Security Analysis in BGP">
    <meta="keywords" content="BGP, Security, Testbed, Mininet, MaxiNet">
    <meta name="author" content="Emerson Barea">
  </head>

  <body>
    <p>Topology: {{ topology.topology }}</p>
    <p><hr></p>
    <p>Peering:</p>
    {% for link in links %}
      <p>
      AS {{ link.autonomous_system1 }} ({{ link.ip_autonomous_system1 }}/{{ link.mask }})
       <->
      AS {{ link.autonomous_system2 }} ({{ link.ip_autonomous_system2 }}/{{ link.mask }})
       bw: {{ link.bandwidth }} Kbps
       load: {{ link.load }} %
       delay: {{ link.delay }} ms
       ({{ link.agreement }})
      </p>
    {% endfor %}
  </body>
</html>